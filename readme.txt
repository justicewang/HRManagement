jsp project:

CRUD operations using JSP/JavaScript
- Perform all the operations in the view page as a single page
- Use JS DOM manipulation to flip to/from view mode to editable mode

in com.dao.UserDAO class 
to change to your database and username and password; run bottom sql code to build the user table for testing.

drop table user;
create table user(username varchar(50), password varchar(15));
insert into user value('Mercy','123');
insert into user value('Abdul','321');

